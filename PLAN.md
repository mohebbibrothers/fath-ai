# نقشه‌ی راه پروژه fath-ai (Project Roadmap)

> هدف: ساخت یک سیستم معاملاتی کمّی **واقعی، حرفه‌ای، و صادقانه** برای بازار
> کریپتو. نه وعده‌ی توخالیِ «وین‌ریت ۱۰۰٪» — بلکه یک موتور که لبه‌ی آماری
> واقعی را *می‌سنجد* و با مدیریت ریسک حرفه‌ای معامله می‌کند.

آخرین به‌روزرسانی: 2026-06-12 | نسخه فعلی: **v0.3**

---

## وضعیت کلی: کجای کار هستیم؟

```
[████████████░░░░░░░░░░] ~45٪  از کل مسیر تا یک سیستم قابل‌اتکا
```

| فاز | توضیح | وضعیت |
|-----|-------|-------|
| **۱. زیرساخت داده** | دریافت داده‌ی واقعی چندمنبعه + کش | ✅ کامل |
| **۲. مهندسی فیچر** | ۴۰ فیچر بدون نشتی داده (leak-free) | ✅ کامل |
| **۳. برچسب‌گذاری** | Triple-barrier (استاندارد طلایی) | ✅ کامل |
| **۴. اعتبارسنجی** | Walk-forward با purge/embargo | ✅ کامل |
| **۵. مدل پایه** | LightGBM + رفع باگ class-alignment | ✅ کامل |
| **۶. بک‌تستر واقع‌گرا** | کارمزد، اسلیپیج، فاندینگ | ✅ کامل |
| **۷. meta-labeling** | مدل دوم «معامله کنیم یا نه» + سایزینگ | ✅ کامل |
| **۸. اخبار/sentiment** | Fear&Greed تاریخی + RSS لایو | ✅ کامل |
| **۹. یادگیری مداوم** | بازآموزی incremental + تشخیص drift | ✅ کامل |
| **۱۰. سوییپ چنددارایی** | تست ۵ ارز × ۳ تایم‌فریم | ✅ کامل |
| **۱۱. مدیریت ریسک پیشرفته** | stop-loss پویا، position sizing بهینه | 🔜 بعدی |
| **۱۲. کاهش drawdown** | regime filter، فیلتر نوسان | 🔜 بعدی |
| **۱۳. بهینه‌سازی هایپرپارامتر** | با CV تودرتو (nested) بدون overfit | ⏳ مونده |
| **۱۴. paper trading لایو** | اتصال به testnet صرافی | ⏳ مونده |
| **۱۵. مانیتورینگ و آلارم** | داشبورد عملکرد + هشدار drift | ⏳ مونده |
| **۱۶. مستندسازی نهایی** | راهنمای کامل + گزارش صادقانه | ⏳ مونده |

---

## دستاوردهای واقعی تا الان (out-of-sample، بعد از کارمزد)

روی **تایم‌فریم روزانه** با meta-labeling — همه Buy&Hold را شکست دادند:

| ارز | بازده ما | بازده Buy&Hold | Sharpe ما | Sharpe B&H |
|-----|---------|---------------|----------|-----------|
| BTC | **+۷۲٪** | −۱۱٪ | ۱.۱۰ | ۰.۰۹ |
| ETH | **+۷٪** | −۵۴٪ | ۰.۴۹ | −۰.۲۳ |
| SOL | **+۳۲۸٪** | −۵۶٪ | ۱.۸۳ | −۰.۳۱ |

**یافته‌ی کلیدی:** لبه‌ی واقعی روی تایم‌فریم‌های بالا (روزانه) وجود دارد؛
روی تایم‌فریم‌های پایین (۱ساعته) کارمزد آن را از بین می‌برد. دقت OOS حدود
۵۱–۵۵٪ است — کوچک ولی واقعی و پایدار در چند بازار.

---

## مشکلات باز که باید حل شوند (صادقانه)

1. **Drawdown بالا (۴۰–۷۲٪):** بزرگ‌ترین مشکل فعلی. نیاز به stop-loss سطح-
   پورتفو و کاهش اهرم در رژیم‌های پرنوسان.
2. **مدل تقریباً همیشه در بازار است:** meta هنوز به‌اندازه‌ی کافی سیگنال‌های
   ضعیف را فیلتر نمی‌کند. باید آستانه‌ها و فیچرهای meta بهتر شوند.
3. **کالیبراسیون احتمالات:** «اطمینان» مدل هنوز کاملاً با دقت واقعی هم‌خوان نیست.
4. **drift رو به کاهش:** سیستم در دوره‌های اخیر افت دقت نشان می‌دهد — باید با
   فیچرهای رژیم‌محور و بازآموزی تطبیقی بهبود یابد.

---

## گام‌های بعدی (به ترتیب اولویت)

1. **مدیریت ریسک پیشرفته** — stop-loss پویا مبتنی بر ATR، سقف drawdown، و
   vol-targeting در سطح پورتفو تا drawdown شکسته شود.
2. **regime filter** — فقط در رژیم‌هایی معامله کن که لبه تاریخاً حاضر بوده
   (مثلاً ترند vs رنج).
3. **بهینه‌سازی هایپرپارامتر با nested CV** — بدون نگاه به داده‌ی تست.
4. **paper trading** — اجرای زنده روی testnet برای اعتبارسنجی نهایی.
5. **داشبورد مانیتورینگ** — رصد لحظه‌ای عملکرد و هشدار drift.

---

## فلسفه‌ی کار (چرا به این روش)

- **هیچ‌وقت خودمان را گول نمی‌زنیم:** هر عدد out-of-sample و بعد از کارمزد است.
- **همیشه مقایسه با Buy&Hold:** سود در بازار صعودی مهارت نیست.
- **پایداری مهم‌تر از یک عدد بزرگ:** لبه‌ای که فقط یک‌بار ظاهر شود، نویز است.
- **ذخیره‌ی ورژن‌به‌ورژن روی گیت:** هر پیشرفت کامیت و پوش می‌شود.
